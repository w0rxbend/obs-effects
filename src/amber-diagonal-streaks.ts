import { AmberDiagonalStreaksScreen } from "./app/screens/DiagonalStreaksScreen";
import { createPage } from "./lib";

createPage(AmberDiagonalStreaksScreen, {
  background: 0x0d0600,
  resizeOptions: { minWidth: 1920, minHeight: 1080, letterbox: false },
});
