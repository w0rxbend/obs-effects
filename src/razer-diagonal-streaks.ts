import { RazerDiagonalStreaksScreen } from "./app/screens/DiagonalStreaksScreen";
import { createPage } from "./lib";

createPage(RazerDiagonalStreaksScreen, {
  background: 0x000800,
  resizeOptions: { minWidth: 1920, minHeight: 1080, letterbox: false },
});
