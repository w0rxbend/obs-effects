import { LuminescentTopographyScreen } from "./app/screens/LuminescentTopographyScreen";
import { createPage } from "./lib";

createPage(LuminescentTopographyScreen, {
  background: "transparent",
  backgroundAlpha: 0,
  fonts: ["1em 'Silkscreen'"],
});
