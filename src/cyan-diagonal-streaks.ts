import { CyanDiagonalStreaksScreen } from "./app/screens/DiagonalStreaksScreen";
import { createPage } from "./lib";

createPage(CyanDiagonalStreaksScreen, {
  background: 0x02080d,
  resizeOptions: { minWidth: 1920, minHeight: 1080, letterbox: false },
});
