import { HandwrittenNotebookScreen } from "./app/screens/HandwrittenNotebookScreen";
import { createPage } from "./lib";

createPage(HandwrittenNotebookScreen, {
  background: 0xf5f0e3,
  fonts: ["400 1em 'Caveat'"],
});
